apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: petclinic
  labels:
    app: frontend
data:
  application.yml: |
    server:
      port: 8080
      compression:
        enabled: true
        mime-types: application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css

    spring:
      application:
        name: frontend
      config:
        import: optional:configserver:${CONFIG_SERVER_URL:http://config-server:8888}
      cloud:
        gateway:
          # フロントエンド専用設定: バックエンドAPIルーティングは全てKong Gatewayに転送
          routes:
            # API Gateway routes (for Angular SPA compatibility)
            # /api/gateway/owners/** -> /api/customer/owners/**
            - id: gateway-owners
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/gateway/owners/**
              filters:
                - RewritePath=/api/gateway/owners/(?<segment>.*), /api/customer/owners/$\{segment}
            
            # /api/gateway/pets/** -> /api/customer/pets/**
            - id: gateway-pets
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/gateway/pets/**
              filters:
                - RewritePath=/api/gateway/pets/(?<segment>.*), /api/customer/pets/$\{segment}
            
            # /api/gateway/visits/** -> /api/visit/visits/**
            - id: gateway-visits
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/gateway/visits/**
              filters:
                - RewritePath=/api/gateway/visits/(?<segment>.*), /api/visit/visits/$\{segment}
            
            # /api/gateway/vets -> /api/vet/vets
            - id: gateway-vets
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/gateway/vets
              filters:
                - RewritePath=/api/gateway/vets, /api/vet/vets
            
            # Customers API - Kong経由
            - id: customers-service
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/customer/**
              filters:
                - StripPrefix=0
            
            # Visits API - Kong経由
            - id: visits-service
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/visit/**
              filters:
                - StripPrefix=0
            
            # Vets API - Kong経由
            - id: vets-service
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/vet/**
              filters:
                - StripPrefix=0
            
            # GenAI API (Java) - Kong経由
            - id: genai-service
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/genai/**
              filters:
                - StripPrefix=0
            
            # GenAI Python API - Kong経由
            - id: genai-python
              uri: http://kong-gateway-proxy.kong.svc.cluster.local:8000
              predicates:
                - Path=/api/genai-python/**
              filters:
                - StripPrefix=0
        discovery:
          enabled: false

    eureka:
      client:
        enabled: true
        serviceUrl:
          defaultZone: ${EUREKA_SERVER_URL:http://discovery-server:8761/eureka}
        registryFetchIntervalSeconds: 5
        registerWithEureka: true
        fetchRegistry: true
      instance:
        preferIpAddress: true
        lease-renewal-interval-in-seconds: 5

    management:
      endpoints:
        web:
          exposure:
            include: '*'
      endpoint:
        health:
          show-details: always
      # Disable Spring Boot's built-in tracing (using OpenTelemetry instead)
      tracing:
        enabled: false
      zipkin:
        tracing:
          endpoint: ""  # Disable Zipkin exporter
      
    # Disable Spring Cloud Sleuth Zipkin (OpenTelemetry handles all tracing)
    # This prevents duplicate traces to localhost:9411
    spring.zipkin.enabled: false
    spring.sleuth.enabled: false

    logging:
      level:
        root: INFO
        org.springframework.cloud.gateway: INFO
        org.springframework.web: INFO

